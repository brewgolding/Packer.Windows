{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
          "label": "packer build",
          "type": "shell",
          "command": "& ${workspaceFolder}\\build.ps1 -BuildType build"
        },
        {
          "label": "packer validate",
          "type": "shell",
          "command": "& ${workspaceFolder}\\build.ps1 -BuildType validate"
        },
        {
          "label": "packer inspect",
          "type": "shell",
          "command": "& ${workspaceFolder}\\build.ps1 -BuildType inspect",
        }
    ]
}
